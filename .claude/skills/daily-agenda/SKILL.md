---
name: daily-agenda
description: >-
  שגרת ניהול יום אישית מהמיילים והיומן של המשתמש. הפעל את הסקיל בכל פעם שהמשתמש
  מבקש "רשימת עבודה", "מה יש לי היום", "סבב בוקר", "סבב ערב", "מה נשאר פתוח",
  וגם בפניות יזומות לניהול משימה: "הוסף משימה" / "משימה חדשה" / "סגירת משימה" /
  "ביצוע משימה" / "עדכון משימה" / "משימה", או מפנה ל"סקיל היומי" / daily-agenda.
  בבוקר: קורא מיילים חדשים ואירועי היום (וגם הצצה ליום מחר) ומפיק רשימת עבודה,
  כשמשימות שנשארו פתוחות מאתמול מתגלגלות לראש הרשימה. בערב: עובר עם המשתמש על
  הרשימה ומסמן מה נסגר ומה נשאר פתוח. תומך גם בהוספה/סגירה/עדכון משימה יזומים
  במהלך היום. הפעל אותו גם כשטריגר מתוזמן שולח "morning routine" או
  "evening routine" של הסקיל הזה.
---

# רשימת עבודה יומית (daily-agenda)

סקיל זה מנהל שגרה יומית קבועה מתוך המיילים והיומן של המשתמש. שלושה מצבים:

1. **בוקר** — ריכוז: קריאת מיילים חדשים + אירועי היום, והפקת רשימת עבודה.
2. **ערב** — סגירה: מעבר על הרשימה עם המשתמש, מה נסגר ומה נשאר פתוח.
3. **גלגול** — כל מה שנשאר פתוח בערב עולה אוטומטית לראש רשימת הבוקר הבא.

**שפה:** כל התוצרים והשיחה בעברית, יישור לימין.

**אזור זמן:** `Asia/Jerusalem`. כל שעה/תאריך מחושבים לפי אזור זה.

**ברירות מחדל** (ניתנות לשינוי בקובץ `state/config.md`):
- מיילים שנחשבים "משימה": לא-נקראים בתיבה הראשית מ-24 השעות האחרונות.
- ימי הפעלה: ראשון–חמישי.
- שעות: בוקר 07:00, ערב 21:00.

---

## קובץ המצב

מצב הסקיל נשמר תחת `state/`:

- `state/current.md` — הרשימה החיה של היום (עם תיבות סימון).
- `state/config.md` — העדפות המשתמש (מיילים שנחשבים, שעות, ימים).
- `state/log/YYYY-MM-DD.md` — ארכיון של רשימת כל יום שנסגר.
- `state/activity-log.xlsx` — קובץ אקסל מצטבר: פעילות לפי יום ומה נעשה.

**מוסכמות סימון בקובץ `current.md`:**
- `- [ ]` — לא בוצע (פתוחה, מתגלגלת למחר).
- `- [x]` — בוצע (נסגרה).
- `- [/]` — בוצע חלקית (מה שנשאר מתגלגל למחר, עם הערה).
- `- [~]` — נדחתה / לא רלוונטית (לא מתגלגלת הלאה).

הערות "מה נשאר" נכתבות בסוגריים בסוף השורה, למשל:
`- [/] מאת ספק — הצעת מחיר (נשאר: לאשר תקציב מול הנהלה)`

**משימה מול מידע (חשוב):**
- **משימה** = פריט שדורש פעולה (מייל לטיפול, אירוע, משימה ידנית). מסומן בתיבת
  סימון `- [ ]`, נספר, עולה בסבב הערב, מתגלגל אם לא נסגר, ונכתב לאקסל.
- **מידע** = פריט שכל עניינו לעדכן/ליידע ואין בו פעולה נדרשת (ניוזלטרים, דייג'סטים,
  עלונים — למשל מיילי נבו על פסיקה/חקיקה/ייצוגיות). נכתב **בלי תיבת סימון**, בקטע
  "📰 מידע", **לא נספר כמשימה, לא עולה בסבב הערב, לא מתגלגל למחר ולא נכתב לאקסל**.
  מוצג בבוקר של אותו יום בלבד. אם המשתמש מבקש במפורש — אפשר להפוך פריט מידע
  למשימה (להוסיף `- [ ]` ולטפל בו ככל משימה).
  **חשוב:** לכל פריט מידע צרף **תמצית קצרה** (שורה–שתיים) של מה שהתחדש בפועל —
  לא רק כותרת. למיילים כבדים (כמו נבו) פתח את גוף המייל, חלץ את עיקרי הפריטים
  והבלט את הרלוונטי למשתמש (כעו"ד: מקרקעין, בנקאות, צרכנות, ייצוגיות).

תמיד קרא את `state/config.md` בתחילת כל ריצה כדי לכבד העדפות עדכניות.

---

## מצב בוקר (morning routine)

כשמתבקש "סבב בוקר" / "רשימת עבודה" / טריגר "morning":

1. **קבע תאריך.** מצא את תאריך היום ואת שם היום בעברית (לפי `Asia/Jerusalem`).

2. **גלגל את הפתוח מאתמול.** קרא את `state/current.md` הקיים (אם יש):
   - אסוף כל שורה עם `- [ ]` (פתוחה) **מקטעי המשימות בלבד** — אלה מתגלגלות.
   - **קטע "📰 מידע" לא מתגלגל** — הוא נבנה מחדש כל בוקר מהמיילים של אותו יום.
   - העבר את הקובץ הישן לארכיון: `state/log/<תאריך-הקודם>.md`.
   - אם `current.md` כבר מהיום, אל תגלגל שוב — רק רענן אירועים/מיילים חדשים.

3. **קרא מיילים חדשים וסווג.** השתמש ב-`mcp__Gmail__search_threads` עם השאילתה
   מ-`config.md` (ברירת מחדל: `in:inbox is:unread newer_than:1d`). **סווג כל מייל:**
   - **לטיפול** (דורש פעולה — דרישה, בקשה, עניין אישי/עסקי) → קטע "מיילים לטיפול"
     עם `- [ ]`.
   - **מידע** (ניוזלטר/דייג'סט/עלון שכל עניינו לעדכן — נבו, פרסומים וכו') → קטע
     "📰 מידע" בלי תיבת סימון.
   לכל פריט: `מאת <שולח> — <נושא>`. אל תיכנס לגוף המייל אלא אם הנושא לא ברור
   או המשתמש ביקש עומק. בספק — סווג כמשימה ושאל את המשתמש.

4. **קרא אירועי היום — ויומיים קדימה.** השתמש ב-`mcp__Google_Calendar__list_events`
   על היומן הראשי (`Asia/Jerusalem`). קרא **מתחילת היום ועד סוף מחר** (טווח של
   יומיים). הפרד: אירועי **היום** לרשימה הפעילה, ואירועי **מחר** לקטע "מחר (הצצה)"
   כדי שהמשתמש יידע מה מחכה לו. כלול אירועי חגים אם רלוונטי. לכל אירוע:
   `<שעה> — <כותרת> (<מיקום/משתתפים אם יש>)`.

5. **הרכב את `state/current.md`** במבנה הזה:

   ```
   # רשימת עבודה — <תאריך> (<יום בעברית>)

   ## פתוח מאתמול
   - [ ] <משימות שהתגלגלו>

   ## אירועי היום (יומן)
   - [ ] <שעה> — <אירוע>

   ## מיילים לטיפול
   - [ ] מאת <שולח> — <נושא>

   ## 📰 מידע (לא מתגלגל, לא משימה)
   - מאת <שולח> — <נושא>   ← עדכון/ניוזלטר, לידיעה בלבד

   ## מחר (הצצה)
   - <שעה> — <אירוע של מחר> (ללא תיבת סימון; רק ידיעה מקדימה)

   ## הערות
   <ריק, למילוי במהלך היום>
   ```

   קטע "מחר (הצצה)" הוא אינפורמטיבי בלבד — לא מגלגלים אותו ולא מסמנים אותו.
   בבוקר הבא האירועים האלה יופיעו ממילא תחת "אירועי היום".

   אם אין פריטים בקטגוריה — כתוב "אין" במקום להשאיר ריק.

6. **מסור למשתמש.** הצג את הרשימה בצ'אט בעברית מסודרת. אם רץ מטריגר מתוזמן
   (המשתמש כנראה לא נוכח), שלח גם `PushNotification` קצרה עם מספר הפריטים
   הפתוחים ושורת הכותרת, ואם המשתמש ביקש ערוצים נוספים ב-`config.md` (מייל /
   Artifact) — הפק גם אותם.

---

## מצב ערב (evening routine)

כשמתבקש "סבב ערב" / "מה נשאר פתוח" / טריגר "evening":

1. קרא את `state/current.md`.

2. **עבור פריט-פריט בצ'אט.** הצג את הפריטים הפתוחים ממוספרים, וקדם אותם
   אחד-אחד (או בקבוצה אם המשתמש מעדיף). לכל פריט שאל בעברית:
   "בוצע / לא בוצע / בוצע חלקית?" — ואם "חלקית", שאל "מה נשאר?".
   קבל תשובות גם בקיצור (מספר + סטטוס), ואל תמהר לפריט הבא בלי תשובה.

3. **הצלבה אוטומטית (עזר):** לפני השאלה, בדוק אם יש רמז לסגירה —
   למשל מייל שנקרא/נענה, או אירוע שעבר. הצע סימון והמתן לאישור. אל תסמן
   `- [x]` בלי אישור המשתמש.

4. עדכן ב-`current.md` לפי התשובות:
   - "בוצע" → `- [x]`.
   - "לא בוצע" → נשאר `- [ ]` (יתגלגל למחר).
   - "בוצע חלקית" → `- [/]` עם הערה `(נשאר: ...)` (החלק שנשאר מתגלגל).
   - "לא רלוונטי / בטל" → `- [~]` (לא מתגלגל).
   הוסף פריטים חדשים אם המשתמש מזכיר.

5. **כתוב לאקסל המצטבר.** הוסף את פעילות היום ל-`state/activity-log.xlsx`
   (ראה סעיף "אקסל פעילות" למטה) — שורה לכל פריט.

6. סכם: "בוצעו X, חלקית Y, נשארו פתוחות Z — יתגלגלו למחר." שמור את הקובץ,
   וקומיט את `current.md`, הארכיון והאקסל.

7. **מסור למשתמש את האקסל.** אחרי הקומיט:
   - שלח את הקובץ בצ'אט עם `SendUserFile` (display attach) לצפייה/הורדה מהנייד.
   - תן קישור GitHub קבוע לגרסה העדכנית:
     `https://github.com/relaf6-glitch/crm/blob/claude/daily-task-management-skill-crttxz/.claude/skills/daily-agenda/state/activity-log.xlsx`
     (עדכן את שם הענף אם השתנה/מוזג ל-main).

---

## אקסל פעילות (`state/activity-log.xlsx`)

קובץ מצטבר עם היסטוריית הפעילות. נכתב בסוף כל סבב ערב (או ידנית לבקשת המשתמש).

**מבנה גיליון אחד בשם `פעילות`, RTL, עם העמודות:**

| תאריך | יום | קטגוריה | פריט | סטטוס | מה נשאר / הערות |
|-------|-----|---------|------|-------|-----------------|

- **קטגוריה:** יומן / מייל / משימה.
- **סטטוס:** בוצע / לא בוצע / בוצע חלקית / בוטל.
- שורה אחת לכל פריט מ-`current.md` של אותו יום.

**כתיבה (append, לא דריסה):** השתמש ב-openpyxl (אם חסר: `pip install openpyxl`,
או הפעל את סקיל `xlsx`). אם הקובץ לא קיים — צור אותו עם
כותרות; אחרת פתח והוסף שורות בהמשך. הגדר `ws.sheet_view.rightToLeft = True`
ליישור עברית. דוגמה:

```python
import os
from openpyxl import Workbook, load_workbook

path = ".claude/skills/daily-agenda/state/activity-log.xlsx"
headers = ["תאריך", "יום", "קטגוריה", "פריט", "סטטוס", "מה נשאר / הערות"]
if os.path.exists(path):
    wb = load_workbook(path); ws = wb["פעילות"]
else:
    wb = Workbook(); ws = wb.active; ws.title = "פעילות"
    ws.append(headers); ws.sheet_view.rightToLeft = True
for row in rows:            # rows = רשימת רשומות היום
    ws.append(row)
wb.save(path)
```

אחרי הכתיבה, ציין למשתמש שהאקסל עודכן, וקומיט אותו.

## ניהול משימות יזום (במהלך היום)

המשתמש יכול לפנות בכל רגע בהודעה יזומה כדי לנהל משימות, בלי לחכות לסבב.

**זיהוי הכוונה לפי הניסוח:**
- "הוסף משימה" / "משימה חדשה" → **הוספה** (דלג ישר לשלב ההוספה).
- "סגירת משימה" / "ביצוע משימה" → **סגירה**.
- "משימה" (בלי הבהרה) → שאל: **הוספה / סגירה / עדכון?** ואז המשך לפי התשובה.

**הוספה:** שאל "מה המשימה?", וקבל תיאור חופשי. הוסף שורה `- [ ]` תחת קטע
**"משימות שנוספו"** ב-`state/current.md` (צור את הקטע אם אינו קיים). מכאן והלאה
המשימה מתנהגת ככל פריט אחר: מופיעה בסבב הערב, מתגלגלת אם לא נסגרה, ונכתבת
לאקסל בקטגוריה **"משימה"**. אשר למשתמש שהמשימה נוספה.

**סגירה:** הצג את הפריטים הפתוחים ממוספרים ובקש לבחור (או קבל את שם המשימה).
סמן `- [x]`, ואם רוצה — כתוב מיד גם לאקסל. אשר מה נסגר.

**עדכון:** בקש לבחור פריט, וקבל את הטקסט/הסטטוס החדש. עדכן את השורה
ב-`current.md` (למשל שינוי ניסוח, מעבר ל-`[/]` חלקי עם "נשאר: ...", או הוספת הערה).

בכל פעולה יזומה: שמור את `current.md`, וקומיט ופוש לענף.

## מודל ההפעלה: טריגרים קשורים-לשיחה

שני Routines מתוזמנים **קשורים לשיחה הראשית** (self-bind, לא סשן טרי), כך
שבזמן הריצה הם **מתעוררים בתוך אותה שיחה** — עם אותה גישה מלאה ל-Gmail
וליומן שיש לשיחה. הם מריצים את הסבב לבד ומפרסמים את התוצאה בשיחה + התראה
לנייד. אין צורך שהמשתמש יענה למשהו.
- בוקר 07:00 (ראשון–חמישי): מריץ את סבב הבוקר בשיחה.
- ערב 21:00 (ראשון–חמישי): מריץ את סבב הערב בשיחה (שואל אותי פריט-פריט).

**למה לא סשן טרי:** טריגר לסשן טרי נפתח בלי חיבורי Gmail/יומן ובלי הקשר
השיחה, ואי אפשר "לענות" להתראה שלו. לכן קושרים לשיחה הקיימת.

**ידני בכל רגע:** אפשר גם פשוט לכתוב בשיחה "סבב בוקר" / "סבב ערב".

**הערת אזור זמן:** התזכורות מתוזמנות ב-UTC. בקיץ (IDT, +3) 07:00=04:00Z,
21:00=18:00Z. במעבר לשעון חורף (IST, +2, בסוף אוקטובר) הן יוקדמו בשעה —
אז יש לעדכן את ה-cron ל-`0 5` ו-`0 19`.

## שימור נתונים וגיזום

- **מקור אמת קבוע:** `state/activity-log.xlsx` בריפו. כל שורה מתועדת בו לתמיד,
  מגורסת ב-git. זהו הארכיון שאסור לאבד.
- **נתונים זמניים:** `state/current.md` (מתגלגל) ו-`state/log/*.md` (ארכיון יומי
  בטקסט). **מותר לגזום/למחוק** קבצי `log/*.md` ישנים בלב שקט — כל המידע כבר
  שמור באקסל. אל תמחק את האקסל.
- **עותק ל-Drive (אופציונלי):** לבקשת המשתמש אפשר להעלות עותק טרי של האקסל
  ל-Google Drive לצפייה נוחה מהנייד (דרך `create_file`). זהו עותק בלבד; מקור
  האמת נשאר בריפו. כלי ה-Drive לא תומכים בעדכון-במקום, לכן לא מנהלים שם מצב חי.

## כשמפעילים ידנית

המשתמש יכול לומר בכל רגע `/daily-agenda` או "תן לי את הרשימה". במקרה כזה זהה
לפי ההקשר אם הכוונה לבוקר (הפקה/רענון) או לערב (סגירה); אם לא ברור — שאל.
