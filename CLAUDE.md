# הוראות עבודה קבועות

הוראות אלה חלות על **כל** תוצר עברי בעבודה עם המשתמש, בכל סקיל ובכל סוג מסמך:
חוות דעת, חוזים, חוברות מחקר, כתבי טענות, ומסמכים פנימיים. הן אינן המלצות.

## שני כללי הברזל של הטיפוגרפיה העברית

### 1. איסור מקפים

אין מקף פיסוק בשום מקום: לא מקף מפריד (em dash), לא מקף מרווח (" - "), ולא מקף
מחבר בין שתי מילים עבריות. במקומו פסיק, נקודה, נקודתיים, או ניסוח מחדש.

מותרים אך ורק: טווח מספרי או טווח שנים (2016-2019, תש"ם-1980, עמ' 12-15), ומקפים
בתוך כותרת מצוטטת, שם מוסד רשמי או כתובת אינטרנט, שהם לשון המקור.

לפני סגירת כל קובץ הרץ סריקת מקפים, ותקן כל ממצא שאינו ברשימת המותר.

### 2. מספור סעיפים: המספר לפני הסעיף, והנקודה אחרי המספר

לכלל הזה **מעמד זהה לאיסור המקפים**.

- **המספר הרץ בא לפני גוף הסעיף, ולא על כותרת הסעיף.** הכותרת נשארת שורה מודגשת
  בלא מספר, והמספר פותח את פסקת הגוף:

  ```
  הכספים, ייעודם והשליטה בהם          (כותרת, בלא מספר)
  7.  כל התרומות שיגויסו בקמפיין ...   (גוף, המספר בפתח)
  ```

- **אחרי המספר באה נקודה: "11." ולא ".11".** זו איננה שאלה קוסמטית אלא באג בידיאיי.
  אם הנקודה והרווחים יושבים בתוך אותה ריצת LTR עם הספרות, הם נסחפים והנקודה מוצגת
  לפני המספר. הכלל הטכני המחייב:
  - **הספרות בריצת LTR נפרדת** (`rightToLeft=false`), ובה הספרות בלבד.
  - **הנקודה והרווחים בריצת RTL** הבאה מיד אחריה.
  - אין לכלול את הנקודה או את הרווחים בתוך ריצת הספרות.

- **מספור שטוח** (1, 2, 3) לכל אורך המסמך, לא עשרוני (לא 1.1 ולא 2.3).

- **תת סעיפים באות עברית** בסוגריים, (א), (ב), (ג), וכולם בריצת RTL אחת, שאם לא כן
  הסוגריים והאות מתהפכים.

- **הפניות פנימיות לפי נושא הסעיף ולא לפי מספרו**, כדי שהמספור יישאר בר שינוי ובלי
  ליצור מקפים בטווחי סעיפים.

לפני מסירה ודא במבנה הקובץ שכל פסקת סעיף ממוספרת בנויה משתי ריצות (ספרות LTR,
ואחריהן נקודה בריצת RTL), ושאין מספר על אף כותרת.

## יישור והפקה

RTL אמיתי דרך `w:bidi`, בלי `w:jc="right"` בפסקת עברית (מנועים מודרניים קוראים
"right" כ"סוף השורה" ומקפיצים את הטקסט שמאלה). למרכוז `center`, ליישור דו צדדי
`both`. גוף ביישור דו צדדי, מספור עמודים בתחתית ממורכז.

## הכלים שמקודדים את הכללים

- `.claude/skills/jewish-state-booklet/scripts/rtl_docx.py`
  (`add_clause_title`, `add_numbered_clause`, `add_letter_item`)
- `.claude/skills/quality-contract/scripts/build_rtl_docx.js`
  (`h`, `clause`, `letterItem`)

השתמש בהם ואל תבנה מספור ידנית, כדי שהכללים ייאכפו מעצמם.
